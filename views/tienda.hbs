<div class="container mt-4">
	<h1>Our articles</h1>
	<hr />

	<div class="row d-flex justify-content-center gap-4">
		{{#each products}}
			<div class="d-flex card justify-content-between col-lg-3 col-md-4 mb-3 p-3" style="width: 18rem;">
				<div>
					<img src={{thumbnail}} class="card-img-top beers-img" alt={{title}} />
					<h2 class="card-title">{{title}}</h2>
					<h5 class="card-subtitle text-secondary">{{price}} €</h5>
				</div>
				<a href="#" class="mt-3 buy-btn btn btn-primary">Buy now</a>
			</div>

		{{/each}}
	</div>

	<a href="/" class="btn btn-primary">Inicio</a>
</div>